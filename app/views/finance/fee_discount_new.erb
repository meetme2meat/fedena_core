


<div id="content-header">
<%= image_tag("/images/show_finance.png") %>
  <h1><%= t('fee_discounts') %></h1>
  <h3><%= t('finance_management') %></h3>
  <div id="app-back-button">
    <%= link_to_function image_tag("/images/buttons/back.png",:border => 0), "history.back()" %>
  </div>
</div>

<div id="inner-tab-menu">
  <ul>

        <li><%= link_to "#{t('view_discount')}", :action=>'fee_discounts'%></li>
      </ul>

</div>

<div id="page-yield">
  <div id="flash_box">
  </div>
  <% unless flash[:notice].nil? %>
    <p class="flash-msg"> <%= flash[:notice] %> </p>
  <% end %>

<div id="form-errors"></div>
    <div id="batch">
    <div class="label-field-pair"><label><%= t('select_discount_type') %> : </label>
        <div class="text-input-bg"> <%= select("id",:id,[["#{t('batch')}","batch_wise"],["#{t('student_category')}","category_wise"],["#{t('student_text')}","student_wise"]],
            {:prompt => "#{t('select_discount_type')}"},
            {:onChange => "#{remote_function(:url => {:action => "load_discount_create_form"}, :with => "'type='+value")}"})%> </div>
   </div>
    </div>

<div id="form-box"></div>

</div>